# Suggested Commands
- `npm install` – install dependencies (project ships `.npmrc` that disables global workspace mode).
- `npm run dev` – launch the Vite dev server for local iteration.
- `npm run build` – run TypeScript project references (`tsc -b`) and build the production bundle used for Netlify deploys.
- `npm run preview` – serve the latest production build locally for smoke testing.